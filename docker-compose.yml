services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_FILE: /data/bot_data.sqlite
      HEALTH_HOST: 0.0.0.0
      HEALTH_PORT: "8080"
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
      - ./logs:/app/logs
